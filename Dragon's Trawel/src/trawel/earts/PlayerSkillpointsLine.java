package trawel.earts;

import derg.menus.MenuLine;

public class PlayerSkillpointsLine extends MenuLine {

	@Override
	public String title() {
		/*
		int num = Player.player.getPerson().getSkillPoints();
		if (num == 0) {
			return "You have no skillpoints.";
		}
		if (num == 1) {
			return "You have 1 skillpoint.";
		}
		return "You have " + num + " skillpoints.";*/
		return null;
	}

}
